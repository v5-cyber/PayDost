services:
  - type: web
    name: paydost
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 24.0.0
      - key: DATABASE_PATH
        value: ./paydost.db
